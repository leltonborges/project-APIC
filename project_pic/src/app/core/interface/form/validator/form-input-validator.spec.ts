import { FormInputValidator } from './form-input-validator';

describe('FormInputValidator', () => {
  it('should create an instance', () => {
    expect(new FormInputValidator()).toBeTruthy();
  });
});
