import { ShowLoggedDirective } from './show-logged.directive';

describe('ShowLoggedDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowLoggedDirective();
    expect(directive).toBeTruthy();
  });
});
