import { DarkerOnHoverDirective } from './darker-on-hover.directive';

describe('DarkerOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new DarkerOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
