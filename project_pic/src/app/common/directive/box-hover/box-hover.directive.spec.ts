import { BoxHoverDirective } from './box-hover.directive';

describe('BoxHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxHoverDirective();
    expect(directive).toBeTruthy();
  });
});
