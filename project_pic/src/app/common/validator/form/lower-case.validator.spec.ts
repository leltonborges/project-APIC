import { LowerCase.Validator } from './lower-case.validator';

describe('LowerCase.Validator', () => {
  it('should create an instance', () => {
    expect(new LowerCase.Validator()).toBeTruthy();
  });
});
